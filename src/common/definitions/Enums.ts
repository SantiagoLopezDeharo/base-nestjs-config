export enum uchatType {
  private = 'private',
  collaborative = 'collaborative',
  public = 'public',
}

export enum FileType {
  image = 'image',
  video = 'video',
  document = 'document',
}

export enum MemberType {
  owner = 'owner',
  collaborator = 'collaborator',
  viewer = 'viewer',
}
